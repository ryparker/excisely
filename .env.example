# Database — local Docker Postgres (default) or Neon for production
DATABASE_URL=postgresql://excisely:excisely@localhost:5433/excisely

# Storage (Vercel Blob)
BLOB_READ_WRITE_TOKEN=

# Auth (Better Auth)
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3000
NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:3000

# OpenAI (optional) — enables GPT-4.1-mini for applicant pre-fill extraction.
# When set, brand_name and fanciful_name extraction improves significantly.
# When unset, the app falls back to rule-based extraction (works but less accurate for proper nouns).
OPENAI_API_KEY=
