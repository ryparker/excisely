# ── Required ──────────────────────────────────────────────────────
DATABASE_URL=postgresql://excisely:excisely@localhost:5433/excisely
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3000
NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:3000

# ── Optional: Cloud AI ───────────────────────────────────────────
# Add these to unlock the Cloud AI pipeline (higher accuracy + bounding box overlays).
# Without these, the app uses the local pipeline (Tesseract.js OCR, zero outbound calls).
# OPENAI_API_KEY=
# GOOGLE_APPLICATION_CREDENTIALS=  # path to GCP service account JSON

# ── Optional: Cloud Storage ──────────────────────────────────────
# Add this to use Vercel Blob instead of local filesystem for image uploads.
# BLOB_READ_WRITE_TOKEN=
